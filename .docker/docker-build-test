FROM alpine:latest

WORKDIR /app

# Copy env file from build context
COPY some.txt some.txt
COPY .env .env

RUN echo "Ohh yehhhhhhhhhhh"
RUN cat some.txt
RUN cat .env 

# Show file inside container
CMD sh -c "\
  echo 'Inside Docker container:'\
"
